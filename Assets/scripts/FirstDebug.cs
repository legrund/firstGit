using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FirstDebug : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log ("firstGit");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
